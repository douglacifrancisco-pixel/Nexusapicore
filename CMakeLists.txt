cmake_minimum_required(VERSION 3.10.0)
project(NexusAPICore)

# Este é o repositório principal
# O build é feito pelo script: scripts/build_complete.sh
# Ou pelo workflow: .github/workflows/build_complete.yml

message(STATUS "NexusAPICore: Repositório de build unificado LTW + ANGLE")
message(STATUS "Execute: ./scripts/build_complete.sh")
